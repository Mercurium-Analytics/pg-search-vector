# Runtime dependencies for pg-search-vector-django.
# Prefer installing via pyproject.toml (`pip install .` or `uv pip install .`)
# to pick up optional extras [pgvector], [openai], [all].
Django>=4.2
requests>=2.28
